services:
  web:
    ports: !override ['80:8080','443:8443']
    volumes:
      - ./infra/nginx/tls.conf:/etc/nginx/conf.d/default.conf:ro
      - ${TLS_DIRECTORY:-./.local/tls}:/etc/nginx/tls:ro
      - ${ACME_DIRECTORY:-./.local/acme}:/var/www/acme:ro
